{
  "name": "@pando/datatable-testing",
  "version": "0.1.1",
  "description": "In-memory adapter implementations and test helpers for the Pando DataTable package",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pando/datatable-contracts": "workspace:*",
    "@pando/datatable-core": "workspace:*"
  },
  "devDependencies": {
    "jsdom": "^29.0.2",
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.3"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
